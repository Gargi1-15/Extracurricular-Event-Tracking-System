# Optional Render Blueprint config. You can also set these in the Render dashboard.
services:
  - type: web
    name: extracurricular-events
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
